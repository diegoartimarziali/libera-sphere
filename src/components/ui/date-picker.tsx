// This component has been deprecated and replaced with a native <Input type="date" />.
// The file is kept to avoid breaking imports, but it should not be used.
// It will be removed in a future cleanup.
export function DatePicker() {
  return null;
}
